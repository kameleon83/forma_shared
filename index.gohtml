<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Index</title>
    <link href="https://fonts.googleapis.com/css?family=Coiny" rel="stylesheet">
    <link rel="stylesheet" href="index.css" media="screen" title="no title">
</head>
<body>
    <div class="container">
        <header>
            <h1>Download file(s)</h1>
            <a href="/upload">Upload File(s)</a>
        </header>
        <section>

            {{ range $k,$v :=  .files }}
            <div class="files">
                {{ if eq $v.Folder "" }}
                <a href="/download/{{ $v.Name }}">{{ $v.Name | title }}</a>
                {{ else }}
                <a href="/download/{{ $v.Folder }}/{{ $v.Name }}">{{ $v.Name | title }}</a>
                {{ end }}
                <span class="size">
                    {{ $v.Size | exp }}
                </span>
                <span class="time">
                    {{ $v.Time | time_fr }}
                </span>
            </div>
            {{ end }}

        </section>
    </div>
</body>
</html>
